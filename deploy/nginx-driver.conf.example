# Example Nginx reverse proxy for City Airport Taxis Driver Dashboard
# Place in /etc/nginx/sites-available/city-airport-taxis-driver

upstream city_airport_taxis_driver {
    server 127.0.0.1:3002;
    keepalive 32;
}

server {
    listen 80;
    server_name driver.example.com;

    client_max_body_size 10m;

    location / {
        proxy_pass http://city_airport_taxis_driver;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
